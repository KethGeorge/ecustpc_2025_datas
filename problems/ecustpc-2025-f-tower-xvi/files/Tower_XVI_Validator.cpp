#include "testlib.h"
#include <bits/stdc++.h>
typedef int64_t LL;
using std::cin, std::cout, std::endl, std::vector, std::pair;

int main(int argc, char *argv[])
{
    registerValidation(argc, argv);
    setName("Validate the input of Tower XVI");
    const int INF = 1'000'000'000;
    int n = inf.readInt(1, 1000, "n");
    inf.readEoln();
    for (int i = 1; i <= n; i++)
    {
        inf.readInt(1, INF, "n");
        inf.readSpace();
        inf.readInt(1, INF, "m");
        inf.readEoln();
    }
    inf.readEof();
}