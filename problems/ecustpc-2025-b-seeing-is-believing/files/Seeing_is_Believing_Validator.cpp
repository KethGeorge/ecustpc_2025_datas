#include <bits/stdc++.h>
#include "testlib.h"
typedef int64_t LL;
using std::cin, std::cout, std::endl, std::vector, std::pair;
int main(int argc, char *argv[])
{
    registerValidation(argc, argv);
    setName("Validate the input of Seeing is Believing");
    int l = inf.readInt(0, 100, "n");
    inf.readSpace();
    int m2 = inf.readInt(0, 1, "m2");
    inf.readSpace();
    int m3 = inf.readInt(0, 2, "m3");
    inf.readEoln();
    inf.readEof();
}
